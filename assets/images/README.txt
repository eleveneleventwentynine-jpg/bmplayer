Place app image assets here (app icon source, splash art, etc).

This placeholder file exists only so git tracks this otherwise-empty
directory -- pubspec.yaml declares assets/images/ as an asset folder,
and git doesn't commit empty directories. Delete this file once you've
added real assets.
